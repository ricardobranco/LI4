@model cv2job.Models.Home
@{
    ViewBag.Title = "Home Page";
}

@if (Request.IsAuthenticated)
{
   @Html.Partial("~/Views/Utilizadores/Index.cshtml", Model.Utilizador);
    
}
else
{
    @Html.Partial("~/Views/Utilizadores/Login.cshtml", Model.Loginmodel);
  
}